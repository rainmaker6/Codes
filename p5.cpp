#include<bits/stdc++.h>
using namespace std;
int main(){
    istream& in(std::cin);
    ostream& out(std::cout);
    ios_base::sync_with_stdio(false);
    in.tie(NULL);
    int *p, m=12; p=&m; cout<<*(&p)<<endl;
    
    return 0;
}
