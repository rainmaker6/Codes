language: cpp
dist: focal
compiler:
- clang
- gcc
matrix:
  fast_finish: true
  allow_failures:
    - compiler: clang
script:
- cd cpp
- mkdir test
- cd test
- cmake ..
- cmake --build test
- ctest --verbose
